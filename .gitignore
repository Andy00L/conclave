# Dependencies
node_modules/

# Next.js (web/)
web/.next/
web/out/
web/build/
web/next-env.d.ts

# Hardhat (contracts/)
contracts/artifacts/
contracts/cache/
contracts/coverage/
contracts/coverage.json
contracts/types/
contracts/fhevmTemp/
contracts/dist/

# Env and secrets (never commit)
.env
.env.*
!.env.example

# Logs and OS files
*.log
.DS_Store

# Agent scratch
.scratch/

# Claude Design drop (raw generated drafts, integrated then discarded)
.design-drop/

# Personal working files: design experiments and submission drafts.
# Kept locally, not in the public repo (untracked with git rm --cached).
docs/design/
docs/submission/
